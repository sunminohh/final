<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.mgv.store.mapper.OrderItemMapper">
    <insert id="insertOrderItem" parameterType="OrderItem">
        INSERT INTO MGV_ORDER_ITEM
        (
            ORDER_ID,
            CAT_NO,
            PRODUCT_NO,
            PRODUCT_AMOUNT,
            PRODUCT_PRICE,
            PACKAGE_NO,
            PACKAGE_AMOUNT,
            PACKAGE_PRICE
        ) VALUES
              (#{orderId}, #{catNo}, #{productNo}, #{productAmount}, #{productPrice}, #{packageNo}, #{packageAmount}, #{packagePrice})
    </insert>
</mapper>